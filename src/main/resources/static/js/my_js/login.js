
function naver_login(){
    console.log('naver-logiin');

    // let uri = 'https://nid.naver.com/oauth2.0/authorize?' +
    //     'response_type=code' +                  // 인증과정에 대한 내부 구분값 code 로 전공 (고정값)
    //     '&client_id=FANlMrwpwNGfW00TctRK' +     // 발급받은 client_id 를 입력
    //     '&state=naver' +             // CORS 를 방지하기 위한 특정 토큰값(임의값 사용)
    //     '&redirect_uri=http://localhost:8080/naverLogin';  // callback url
    //
    // // 사용자가 사용하기 편하게끔 팝업창으로 띄어준다.
    // window.open(uri, "Naver Login Test PopupScreen", "width=450, height=600");



}